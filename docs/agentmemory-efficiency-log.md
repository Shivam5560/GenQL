# agentmemory efficiency log

Tracks whether handing a session off through the `agentmemory` plugin (save state → new session →
`memory_recall`/`memory_smart_search`) actually reduces token/tool-call cost versus re-deriving
context from the repo and prior conversation. One entry per handoff. Be honest about partial or
failed recalls — the point of this log is to let a later review answer, with evidence, whether the
practice is worth keeping.

**Efficiency % definition (working definition, refine if a better one emerges):**
`100 * (1 - (tool calls spent re-deriving context after recall) / (estimated tool calls a from-
scratch re-derivation of the same context would have cost))`. A recall that fully resumed the task
with zero re-exploration scores 100%; a recall that turned out irrelevant and required full
re-derivation anyway scores 0%.

| Date | Session / task | Memories saved (project) | Recalled successfully? | Re-derivation needed after recall | Efficiency % | Notes |
|---|---|---|---|---|---|---|
| 2026-09-06 | GenQL Phase 4 design (this session, pre-handoff) | 5 entries saved to project `genql`: Phase 4 scope, schema, ports/registries, CLI/service shape, Phase 3 closure status | n/a — no handoff has occurred yet in this session | n/a | n/a | Baseline row only, recording what was saved. First real efficiency measurement happens at the next actual handoff, once a fresh session recalls these and we can compare against what re-deriving them would have cost (this session's own exploration took roughly a dozen file reads plus two full spec documents to arrive at these decisions). |
| 2026-09-06 | GenQL Phase 4 SDD execution (subagent-driven-development run, same session continued) | 4 more `memory_save` calls (9 total for the day): implementation-plan-written workflow, SDD-execution-started workflow, and the load-bearing Batch-2 composition-root ruling (bug). | n/a — saving side only, no recall performed in-session | n/a | n/a | Save discipline lapsed partway through: nothing was saved for Batches 3–7 (Tasks 7–12), the unplanned `composition_root.py` → `genql/composition/*_container.py` split, or the "all tasks complete, proceeding to final review" state. The only durable record of that later stretch is the file-based ledger, `.superpowers/sdd/2026-09-06-genql-phase-4-semantic-store-and-retrieval/progress.md`. |
| 2026-09-06 | Post-`/clear` resume: check Phase 4 review status and decide on merge (new session, same day) | 0 saved so far this session (recall/search only until this task's memory-write step) | Partially. `memory_recall`/`memory_smart_search` (2 calls) returned all 9 prior entries, correctly covering Phase 4 scope/schema/registry/CLI design decisions — but recall was called *after* file exploration had already answered the actual question, not before, and the 9 entries stop at the Batch-2 ruling so they could not answer "is Phase 4 done and ready to merge" at all. | Full re-derivation of current execution status was still required regardless of recall: `find`/`ls` for phase-4 files (2 calls), reading the SDD ledger `progress.md` (1 call), `git log`/`git branch` (1 call) — 4 tool calls that recall could not have replaced given the stale saves. `memory_sessions`/`memory_audit` also came back empty (0 tracked sessions/audit entries for this project). | ~25% | Two separate findings: (1) process gap — recall should have been the *first* call this session, not called after the files already answered the question (now corrected, see the `context-handoff-via-agentmemory` memory); (2) data gap — even called first, recall alone would have gotten roughly the design-background half of the picture "for free" (saving ~2-3 file reads) but 0% of the current-status half, because in-session saves did not keep pace with the SDD execution's later batches. Actionable fix for next long SDD run: save an updated workflow entry after every batch (or at minimum after the final batch and after the review), not only at the start. |
| 2026-09-06/07 | GenQL Phase 5 (generation core): brainstorm → spec → plan → 13-task SDD execution → merge to master, same session continued from the row above | 1 `memory_save` call total for the entire phase, made only at the very end (Phase 5 completion summary: scope, deferred items, final-review outcome, the no-feature-branch process note). Zero saves during any of the 13 tasks or 7 batches. | No recall attempted at all during Phase 5 work — the earlier-corrected "recall first" habit from the row above was not applied here because this phase never hit a session boundary (no `/clear`, no handoff); all context stayed live in one continuous session. | n/a — nothing to re-derive since no recall was attempted; the whole phase ran on live conversation context instead of agentmemory. | n/a | Confirms the pattern is still schema-driven by session boundaries, not usage: agentmemory gets used at the start/end of long stretches but not *during* them. This is the same "save discipline lapses mid-execution" gap the Phase 4 rows already found, now also true for saves-during-a-live-session (not just saves-that-go-stale-before-a-later-recall). The user asked directly (2026-09-07) for this log to be "maintain[ed] frequently" — actionable fix, now a standing memory: save one `memory_save` checkpoint after each SDD batch (or at minimum after each phase's final review), not only at the very start and very end of a multi-hour stretch of work. |
| 2026-09-07/08 | GenQL Phase 6 (clarification and multi-turn): Opus scoping consult → spec → plan → 13-task SDD execution → final-review fix wave → merge to master, same continuous session | 3 `memory_save` calls spread across the phase this time (spec-decision checkpoint after writing the spec, one mid-execution checkpoint after Batch 4, one completion summary after merge) — the fix applied from the row above. Still 0 recalls (no session boundary crossed). | No recall needed or attempted — same reasoning as the Phase 5 row; the fix here is about save *frequency*, not about exercising recall, since recall's value only shows up across a boundary this run never crossed. | n/a | n/a | Real improvement over Phase 5's single end-of-phase save, though still short of "after every batch" (3 saves across 7 batches). The mid-execution checkpoint would have mattered this time: Phase 6 hit two mid-run interruptions (a session-wide rate limit on both Opus and Sonnet, and a shared-working-tree staging race across parallel implementers) that a stale save wouldn't have covered if this session had been lost — the file-based SDD ledger, not agentmemory, was what actually carried recovery information through both incidents. Reinforces the standing lesson: agentmemory's realistic role here is a background safety net for session loss, while the file-based ledger is what execution actually depends on turn-to-turn. |
